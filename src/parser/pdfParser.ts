/**
 * @deprecated Mantido só por compat — use parseSupplierFile, que agora
 * lida com todos os formatos (incluindo PDF) via extractTextFromFile.
 */
export { parseSupplierFile as parsePdf } from './supplierTextParser';
